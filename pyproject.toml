[build-system]
requires = ["flit_core >=3.12.0"]
build-backend = "flit_core.buildapi"

[project]
name = "woeai"
authors = [{name = "Chao Li", email = "lichaosz@qq.com"}]
dynamic = ["version", "description"]
